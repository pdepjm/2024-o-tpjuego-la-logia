import wollok.game.*
import wollok.lang.*
import escenarios.*
import elementos.*


program WollokRoad {
	game.title("Wollok Road")
	game.height(25)
	game.width(44)
	game.cellSize(40)

	const portada = new Background()
	escenario.iniciarNivel(portada) 

	game.start()
}

